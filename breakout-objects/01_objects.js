// object => block of code; collection of key/value pairs

const name = 'alice';
const cohort = 'mar 04 2024';
const hasGraduated = false;

// arrays are collections of things
// objects describe one thing
const studentOne = ['alice', 'mar 04 2024', false];

const studentObj = {
  name: 'alice',
  cohort: 'mar 04 2024',
  hasGraduated: false,
};

studentObj = [];

// bracket notation
studentObj['cohort']; // 'mar 04 2024'

arr[2];
arr[2] = 42;

studentObj.cohort;
studentObj.cohort = 'something else';

// dot notation
studentObj.cohort; 
console['log']

const key = 'cohort';
studentObj[key];

studentObj.key;
